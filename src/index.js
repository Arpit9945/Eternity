import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Eternity_vision';
import './style/global.scss'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
    
